<!-- ============================================================================
     InDows module: locale-keyboard
     Sets language, region (formats) and keyboard layout automatically, so the
     first-run language/keyboard screens are skipped.

     WHERE: settings pass="oobeSystem" - ADD this whole component.
     ANCHOR (a comment in the base) named:   [InDows:module] oobe-international

     REPLACE __LOCALE__ with the four values below (see README.md for the code tables). The InDows
     GUI fills it from a language dropdown; for manual use, paste the four elements. English (US) example:

       <InputLocale>0409:00000409</InputLocale>   keyboard layout(s), as "langID:layoutID"
       <SystemLocale>en-US</SystemLocale>         language for non-Unicode programs
       <UILanguage>en-US</UILanguage>             Windows display language (needs a language pack)
       <UserLocale>en-US</UserLocale>             number/date/currency formats
     ============================================================================ -->
<component name="Microsoft-Windows-International-Core" processorArchitecture="amd64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
    __LOCALE__
</component>
