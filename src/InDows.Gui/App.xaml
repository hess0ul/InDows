<Application x:Class="InDows.Gui.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:InDows.Gui.ViewModels"
             xmlns:views="clr-namespace:InDows.Gui.Views"
             xmlns:nav="clr-namespace:InDows.Gui.Navigation">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Theme/ModernTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <nav:InverseBooleanConverter x:Key="Not"/>
            <nav:StringEqualsConverter x:Key="IsScreen"/>
            <nav:EnumToBoolConverter x:Key="EnumBool"/>
            <nav:RiskToBrushConverter x:Key="RiskBrush"/>

            <!-- The content area picks the view for whatever view-model is current. -->
            <DataTemplate DataType="{x:Type vm:HomeViewModel}">
                <views:HomeView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:RestoreViewModel}">
                <views:RestoreView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ProfileViewModel}">
                <views:ProfileView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:BuildViewModel}">
                <views:BuildView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
                <views:SettingsView/>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
